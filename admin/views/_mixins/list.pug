mixin list(items)
  ul.list-group
      each item in items
        li.list-group-item
          span= item.title
          .row-links
            a(href=`/admin/${item._id}/update`) Изменить
            a(href=`/admin/${item._id}/delete`) Удалить