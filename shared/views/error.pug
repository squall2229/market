extends _layout

block header

block content
    main#main
        i.error-icon.material-icons error
        if error
            h1= error.message
            pre= error.stack
        else
            h1= message
            p Упс, произошла ошибка.